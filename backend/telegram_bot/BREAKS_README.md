# 📦 Брейки - Документация

## Описание

Брейк — это формат аукциона, в котором пользователи делают ставки на группы (категории карт). После окончания аукциона администратор открывает бустеры, и победитель каждой группы получает ВСЕ карты из выигранной категории.

## Архитектура

### Модели БД

1. **Break** — основной брейк (аукцион)
   - Название, описание, ссылка на чек-лист
   - Статус (черновик, запланирован, активен, завершён, отменён)
   - Время начала и окончания
   - Информация о посте в канале

2. **BreakGroup** — группа карт в брейке
   - Название группы
   - Минимальная ставка и шаг ставки
   - Порядок отображения

3. **BreakBid** — ставка пользователя
   - Группа, пользователь, сумма
   - Флаг валидности (текущая максимальная ставка)

4. **BreakWinner** — победитель группы
   - Группа, пользователь, выигрышная ставка
   - Флаг уведомления

### Основные модули

- **`models.py`** — модели БД
- **`breaks.py`** — основная логика брейков:
  - Меню брейков
  - Просмотр брейка и групп
  - Процесс ставок
  - Уведомления
  - Работа с комментариями в канале
  - Завершение брейков

- **`bot.py`** — интеграция с Telegram-ботом:
  - Обработчики команд и callback'ов
  - Deep links для брейков

- **`admin.py`** — админ-панель для управления брейками

- **`tasks.py`** — автоматическое завершение брейков

## Установка и настройка

### 1. Создание миграций

```bash
cd backend
python manage.py makemigrations telegram_bot
python manage.py migrate
```

### 2. Настройка переменных окружения

В `.env` или `settings.py` добавьте:

```python
TELEGRAM_BOT_TOKEN = "your_bot_token"
TELEGRAM_BOT_USERNAME = "your_bot_username"  # Без @
TELEGRAM_CHANNEL_ID = -1001234567890  # ID канала (отрицательное число)
```

### 3. Настройка периодической проверки брейков

Добавьте в cron (каждую минуту):

```bash
* * * * * cd /path/to/backend && python manage.py check_breaks
```

Или используйте Celery с периодическими задачами.

## Использование

### Для администратора

1. **Создание брейка** (через админ-панель):
   - Перейдите в админку → Брейки → Добавить брейк
   - Заполните название, описание, ссылку на чек-лист
   - Установите время начала и окончания
   - Добавьте группы (инлайн)

2. **Настройка групп**:
   - Название группы (например: "Кот Ик")
   - Минимальная ставка (по умолчанию 1₽)
   - Шаг ставки (по умолчанию 50₽)
   - Порядок отображения

3. **Публикация брейка**:
   - Сохраните брейк
   - Выберите действие "Опубликовать в канал"
   - Бот автоматически создаст пост с кнопкой

4. **Завершение брейка**:
   - Брейк завершится автоматически по истечении времени
   - Или используйте действие "Завершить брейки" в админке

### Для пользователей

1. **Участие в брейке**:
   - Перейдите по ссылке из канала или нажмите кнопку "📦 Брейки" в боте
   - Выберите активный брейк
   - Выберите группу
   - Сделайте ставку

2. **Уведомления**:
   - При перебивании ставки пользователь получит уведомление
   - После завершения победители получат уведомление о победе

## Логика работы

### Ставки

- Минимальная ставка = текущая максимальная ставка + шаг ставки
- При новой ставке предыдущие ставки помечаются как недействительные
- Если ставка сделана менее чем за 5 минут до окончания, время продлевается на 5 минут

### Комментарии в канале

- При каждой новой ставке бот отправляет новый комментарий под постом
- Формат комментария:
  ```
  1 - 300
  2 - 300
  3 - 500
  ```
- Где число — номер группы, второе число — текущая максимальная ставка

### Завершение брейка

- Автоматически по истечении времени (через `check_breaks`)
- Определяются победители для каждой группы
- Победители уведомляются автоматически
- Статус брейка меняется на "завершён"

## API и функции

### Основные функции в `breaks.py`

- `breaks_menu()` — главное меню брейков
- `break_view()` — просмотр брейка
- `break_group_view()` — просмотр группы
- `break_bid_start()` — начало процесса ставки
- `break_bid_process()` — обработка ставки
- `notify_bid_outbid()` — уведомление о перебитой ставке
- `update_channel_comment()` — обновление комментария в канале
- `complete_break()` — завершение брейка
- `notify_winner()` — уведомление победителя
- `format_break_post()` — форматирование поста для канала

## Troubleshooting

### Брейк не завершается автоматически

- Проверьте, что команда `check_breaks` запускается периодически
- Проверьте логи на наличие ошибок

### Уведомления не отправляются

- Проверьте `TELEGRAM_BOT_TOKEN` в настройках
- Проверьте, что пользователи не заблокировали бота
- Проверьте логи на наличие ошибок Telegram API

### Комментарии не обновляются

- Проверьте, что `channel_id` и `channel_post_id` установлены
- Убедитесь, что бот имеет права на отправку сообщений в канал
- Проверьте логи на наличие ошибок

## Дополнительные возможности

### Расширение функционала

- Добавление фото к брейкам
- История всех ставок пользователя
- Статистика по брейкам
- Интеграция с платежной системой
- Автоматическая отправка уведомлений администратору

## Безопасность

- Защита от race condition при ставках (используется `transaction.atomic()`)
- Валидация сумм ставок
- Проверка активности брейка перед принятием ставки
- Логирование всех операций
